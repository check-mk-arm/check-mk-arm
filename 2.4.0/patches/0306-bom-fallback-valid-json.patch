--- omd/Makefile
+++ omd/Makefile
@@ -230,9 +230,13 @@
 	chmod 644 $(DESTDIR)$(OMD_ROOT)/share/doc/COMMIT
 
 # This file is normally generated by our CI chain. In case it is not there, just
-# create an empty one instead of failing the build.
+# create an empty one instead of failing the build. It has to be an empty *bill
+# of materials* rather than an empty file: //omd:generate_bom_csv feeds it to
+# json.load() and then reads bom_info["components"], so a zero-byte file fails
+# the build with JSONDecodeError, which is exactly what this fallback is meant
+# to avoid.
 $(BOM):
-	$(TOUCH) $@
+	echo '{"components": []}' > $@
 
 # Perform some validations
 	for SKEL_DIR in tmp var etc; do \
