--- bazel/toolchains/cc/BUILD
+++ bazel/toolchains/cc/BUILD
@@ -88,6 +88,12 @@
     "objcopy": "/opt/gcc-14.2.0/bin/objcopy",
 }
 
+linux_gcc14_aarch64_cxx_builtin_include_directories = [
+    "/usr/lib/gcc/aarch64-linux-gnu/14/include",
+    "/include",
+    "/usr/include",
+]
+
 ci_cxx_builtin_include_directories = [
     "/opt/gcc-14.2.0/lib/gcc/x86_64-pc-linux-gnu/14.2.0/include",
     "/opt/gcc-14.2.0/lib/gcc/x86_64-pc-linux-gnu/14.2.0/include-fixed",
@@ -101,11 +107,22 @@
         linux_gcc14_cxx_builtin_include_directories,
         linux_gcc14_tool_paths,
         linux_gcc14_link_flags,
+        "k8",
+        "@platforms//cpu:x86_64",
     ),
     "ci": (
         ci_cxx_builtin_include_directories,
         ci_tool_paths,
         ci_link_flags,
+        "k8",
+        "@platforms//cpu:x86_64",
+    ),
+    "linux_gcc14_aarch64": (
+        linux_gcc14_aarch64_cxx_builtin_include_directories,
+        linux_gcc14_tool_paths,
+        linux_gcc14_link_flags,
+        "aarch64",
+        "@platforms//cpu:aarch64",
     ),
 }
 
@@ -116,7 +133,7 @@
         abi_version = "local",
         compile_flags = COMPILE_FLAGS,
         compiler = "gcc",
-        cpu = "k8",
+        cpu = cpu,
         cxx_builtin_include_directories = cxx_builtin_include_directories,
         cxx_flags = CXX_FLAGS,
         dbg_compile_flags = DBG_COMPILE_FLAGS,
@@ -135,6 +152,8 @@
         cxx_builtin_include_directories,
         tool_paths,
         link_flags,
+        cpu,
+        _cpu_constraint,
     ) in TOOLCHAINS.items()
 ]
 
@@ -155,11 +174,11 @@
 [toolchain(
     name = identifier,
     exec_compatible_with = [
-        "@platforms//cpu:x86_64",
+        TOOLCHAINS[identifier][4],
         "@platforms//os:linux",
     ],
     target_compatible_with = [
-        "@platforms//cpu:x86_64",
+        TOOLCHAINS[identifier][4],
         "@platforms//os:linux",
     ],
     toolchain = ":" + identifier + "_toolchain",
